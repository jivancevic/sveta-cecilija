# CLAUDE.md

> **This file is a map + hard rules. Detail lives one click away** in `docs/agents/*.md` and `docs/adr/*.md` — follow the pointers instead of duplicating their content here. Facts owned by code or an ADR are *linked, not copied* (inline copies drift stale). When you're tempted to add a multi-paragraph gotcha, put it in the relevant `docs/agents/` file and leave a one-line pointer.

## Agent docs index

| Topic | Where |
|---|---|
| Issue tracker (GitHub Issues) | `docs/agents/issue-tracker.md` |
| Triage labels (five-role vocabulary) | `docs/agents/triage-labels.md` |
| Schema + DB patterns — bootstrap, enum migrations, atomic & race-safe SQL, TRUNCATE CASCADE | `docs/agents/db-bootstrap.md` (+ `db/schema/README.md`) |
| Deployment + DB topology — Coolify, Dockerfile/standalone build, env promotion, dev/prod/staging DB names | `docs/agents/deployment.md` |
| Working in worktrees / parallel sessions — `.env.local`, devDeps, push-hang, `gh pr merge` | `docs/agents/worktree-dev.md` |
| Permissions & access: the vocabulary, migration bundles, field locks, shared accounts | `docs/agents/permissions.md` (+ [ADR-0023](docs/adr/0023-permissions-replace-roles-app-surface.md)) |
| Payload admin customization (v3) — component paths, importMap, CSRF gate | `docs/agents/payload-admin.md` |
| Frontend & CSS gotchas — specificity, `backdrop-filter`, hero loading, `next/image` | `docs/agents/frontend-css.md` |
| Assets pipeline — `public/` vs `assets/`, webp conversion | `docs/agents/assets.md` |
| Cecilija, the staff and member app (`/app`, called Moreškant until #489) — route group, shared cookie, access decision, roster loaders, PWA, the target route map | `docs/agents/moreskant-app.md` (+ [ADR-0027](docs/adr/0027-cecilija-one-staff-app-payload-backoffice.md), [ADR-0024](docs/adr/0024-moreskant-roster-domain.md)) |
| Feature design notes — offline sales at the door + legacy ([ADR-0025](docs/adr/0025-offline-sales-ledger.md)), non-public performances (`kind`/`isPublic`, ADR-0024), #94 bad-weather venue change, show reschedule + ticket reissue (#379), show cancellation with automatic refunds (#497), #57 marketing opt-outs + review-email attendance gate (#378), Stripe disputes (#380) | `docs/agents/features.md` |
| Domain glossary | `CONTEXT.md` (single context; see `docs/agents/domain.md`) |
| Architecture decisions | `docs/adr/` |

## Hard rules (don't violate; each enforced where noted)

- **Every URL path segment is English, page routes and API routes alike** (#481). Croatian stays where it is the domain: UI copy, DB enum values (`storno`, `zimsko-kino`), cookie names (`moreska_locale`), glossary terms and code symbols. A Croatian segment that already ships gets renamed with a 308 from the old path for one release (`next.config.ts`), never a silent break. The `/app/*` Croatian segments were the one grandfathered set; #495 renamed every screen that existed, and two of those 308s (`/app/instalacija`, `/app/prijava`) are permanent because a wall QR and an SMS link cannot be edited.
- **Cecilija is one app on one host, and the two surfaces have names** ([ADR-0027](docs/adr/0027-cecilija-one-staff-app-payload-backoffice.md)). The staff and member app is **Cecilija** at `moreska.eu/app`, inside this same Next.js app and this same Coolify service; the raw-edit Payload UI at `/admin` is the **Backoffice**, behind `dev` (plus `editor` for Objave and FAQ). Never call either surface "admin" in docs or copy, never add a second container, repo or deploy for the app, and never hand-write a Traefik label for `app.moreska.eu` (that host is inert by choice, #479; the 301 lives in `src/lib/app-subdomain.ts`). Data access for a rebuilt Cecilija screen goes through the repository seam `src/lib/repo/` (`getRepo()`; only `src/lib/repo/payload/` may import `payload` or `@payload-config` — a file scan in `src/lib/repo/repo-guard.test.ts` enforces it, with an allow-list that shrinks per screen). Writes stay on Payload's local API inside the seam so the collection hooks keep running, and field-level access never applies to them, so a form that must refuse a field refuses it in its route. Adopted screen by screen, never as a sweep.
- **Never build internal URLs with a `/en` or `/hr` prefix** — locale is cookie-based, those routes 404 (`src/proxy.ts`).
- **Never query the Shows collection directly in page components** — go through `getUpcomingShows()` / `getNextShow()` in `src/lib/shows.ts`.
- **There is no `capacity` field on Shows and never add one** — capacity is fixed per venue (`VENUE_CAPACITY` in `src/lib/shows.ts`).
- **Ticket prices are fixed: €20 adult, €10 child** — no dynamic pricing. Two sanctioned exceptions, both of which record *why* the price moved and neither of which adds a price category: a **member promo code** overriding the adult price online (ADR-0018), and an **offline sale line** priced below face value, which must carry a `discount_label` ([ADR-0025](docs/adr/0025-offline-sales-ledger.md)). The constants stay the source of truth for what to charge; the ledger records what was charged.
- **There are no roles: a user holds a *set* of permissions** (ADR-0023). The vocabulary is `users | tickets | refunds | door | partner | season_stats | moreska | moreskant | finance | editor | dev`, spelled out once in `src/lib/access/permissions.ts`; never re-type it elsewhere. `can(user, 'refunds')` (or `hasAny`) is the **only** predicate: no role string comparison, no tier alias. "Superadmin" is shorthand for "holds every permission", not a thing in code. `shared` on Users is the only marker of a shared login (ADR-0022); a shared account may not edit its own record. **`can()` is the only predicate of a PERMISSION; `keepsList()` is the only predicate of a ROW** ([ADR-0029](docs/adr/0029-list-keeper-a-permission-that-hangs-on-a-row.md)): one evening's list may be delegated to a moreškant (the *Zaduženi*, `shows.listKeepers`), so `src/lib/app/list-keeper.ts` answers "may this person keep the list of THIS evening" and nothing else. The three routes that take it (`POST /api/app/lineup`, `/lineup/confirm`, `/api/app/attendance`) keep the house guard — `requirePermission(req, ['moreska','moreskant'])` — and check the row AFTER loading it; never widen the permission instead, and never fold `keepsList` back into `can()`, which would make one evening's delegation permanent and global. The alarm is deliberately NOT delegated: `/api/app/alarm` stays `requirePermission(req, 'moreska')`. Details: `docs/agents/permissions.md`.
- **Staff mutation routes must re-check the permission in the handler.** Payload's local API runs `overrideAccess: true`, so collection `access` doesn't gate them. Use `requirePermission(req, 'refunds')` from `src/lib/access/route-guard.ts` (the single chokepoint); don't re-type `getPayload → auth → permission check` inline. The routes whose audience is not a permission but an **account** carry `requireAppSession` from `src/lib/app/session-guard.ts`, which composes Payload's `auth` with the same `decideAppAccess` the page gate uses — still an in-handler re-check, not an exception, and never a re-typed list of permission words. There are three: #496's notification inbox (`POST /api/app/notifications/[id]/read` and `/read-all`), because a notification is addressed to an account, and #650's `POST /api/app/session/renew`, because a session belongs to whoever holds it. There are exactly three kinds of exception, and a route outside them that skips the guard is a bug: **token- or signature-authenticated** routes (Stripe webhook, `/scan/[token]/claim`, `/order/[token]/refund`, unsubscribe, cron, and since #463 `POST /api/app/session`, where the invitation token IS the authentication); the **`/app` routes that have no session yet by nature** (`login`, `logout`, `forgot`, and #463's public `join` and `join/status`), which carry `rejectAppRequest` from `src/lib/app/request-guard.ts` instead and whose rules live in their pure handlers; and the two that authenticate a caller before there is a session to guard — `POST /api/app/authorize` and `/api/mcp` — which re-check `can(user, 'moreska')` inline by design (`docs/agents/permissions.md`).
- **Accumulating numeric columns must use atomic SQL** (`col = col + $1`), never read-modify-write. See `db-bootstrap.md`.
- **Secrets (Stripe live keys, `BREVO_API_KEY`, `PAYLOAD_SECRET`) never appear in any committed file or chat** — runtime env only (Coolify); if one leaks, rotate it.
- **`.gitignore` blocks every `.env*` except `.env.example`** (the committed template).
- **Don't relax the security baseline** — `next.config.ts` security headers + `payload.config.ts` fail-fast on missing `PAYLOAD_SECRET`. That baseline is what lets `/scan/[token]` be safely public.
- **An agent may merge its own PR once every check is green, and never a moment before** (granted 2026-09-12, replacing the rule that reserved the merge click for a human). Since 2026-09-12 a push to `main` deploys straight to production ([ADR-0026](docs/adr/0026-production-auto-deploy-from-main.md)), so with the human checkpoint gone CI is the only gate left in front of a system taking real card payments: wait for all **five** checks to report (`lint-types`, `vitest`, `build`, `docker`, `drift` — `prod-install` was retired by #649 because it mirrored an install Coolify stopped doing at ADR-0012), read them, then `gh pr merge`. A pending check is not a passing one. **Never force-merge past a failing check and never use the admin bypass** — Coolify deploys on the push webhook, not on CI success, so a bypass reaches production directly. Since #649 this is a **lock, not only a rule**: `enforce_admins` is `true` ([ADR-0029](docs/adr/0029-ci-is-the-last-gate-and-the-bypass-is-closed.md)), there is no emergency path, and the gate costs about three minutes because the checks run in parallel. If CI is *broken* rather than failing, the recovery is to turn `enforce_admins` off, ship, and turn it back on — two conscious acts with an audit trail, never a reflex.
- **The gate is the checks; the wait is the rollout.** Merging is not shipping: a push to `main` starts a Coolify build of ~3-4 minutes before the change is on a phone, so "merge → look at it" is a ~7 minute loop and only half of that is CI. Two rules protect the second half. **Staging does not auto-deploy** (#649): `dev.moreska.eu` still tracks `main` but ships on a click, because since ADR-0026 a staging build off the same branch verifies nothing production will not find seconds later, while stealing half of a 4-vCPU box from the build that matters. And **a docs-only change neither gates nor deploys**: `ci.yml`, `docker.yml` and `schema-drift.yml` each decide internally that only `docs/**` or `*.md` moved and report green without running, and Coolify's `watch_paths` excludes the same set. The accepted consequence is that the footer's build line and `/api/health` report **the commit in the image**, not the tip of `main`.
- **Customer support is email only, and no buyer-facing surface carries a phone number** (#383, decided by the uprava 2026-09-13). One address, `info@moreska.eu`, answered within 24 hours; nobody is on duty and there is no helpline. The registry number is the president's private mobile, so putting it back in the footer, in the Organization JSON-LD's `telephone`, or on any page a buyer reads is a bug, not a courtesy (#548 removed exactly that). The **impressum is the one exception and must keep it**: ZZP art. 46(1) point 2 lists a telephone number among the pre-contractual details a trader owes a consumer in a distance contract, and unlike the email address it carries no "ako postoji" qualifier. The "Need help?" block that states this to buyers lives on three surfaces and nowhere else; its seams and its copy are in `docs/agents/features.md`.
- **An artifact that LEAVES the building is accepted in the reader's tool, never on the screen that produced it** (#599). A CSV is opened in Croatian Excel, a PDF is looked at, a letter is read in a mail client. Obračun shipped wrong in five ways at once while every arithmetic test passed, because the tests asserted the figures and nobody asserted the document: the CSV was comma-separated English with no BOM, which the one machine it exists for opens as a column of mojibake. Two corollaries, and both are where the misses actually came from. **A port must name the artifact, not just the screen**: when a change moves a surface to a new audience (#505 took the partner screens from the English Backoffice to Croatian Cecilija), everything that audience RECEIVES is in scope even when no code has to change for the screen to work — "the routes stay as they are" is a clean engineering boundary that cuts the user's journey in half. And **a promise in an `admin.description` is not a fact**: `Partners.oib` claimed for three months to appear on a statement that never carried it, because nothing compared the claim to the renderer. The cheapest instrument for all of it is asking the person who RECEIVES the document, before building it.
- **A sheet is portalled to the body and never trusts a z-index to leave its screen** (#624). Why, and the two flex/overflow floors that come with it: `docs/agents/frontend-css.md`.
- **Never gate a deploy on a health check that fails when the database blips.** `/api/health` is liveness, never readiness: it answers 200 whenever the server is up and reports `dbOk` in the body without letting it change the status code. A failed `bootstrap-db.mjs` is already caught, because the container's `CMD` means `server.js` never starts. Rationale in `src/lib/health/health.ts`.

---

## Project: moreska.eu — HGD Sveta Cecilija

Website for HGD Sveta Cecilija, a 143-year-old cultural organisation from Korčula, Croatia, home of the Moreška sword dance. Public site at `moreska.eu`. Full PRD in GitHub Issues #1. **Cutover is complete (verified 2026-06-25):** `moreska.eu` is live and taking real Stripe sales; both legacy domains 301-redirect to it. The project is now in the operate / go-to-market / compliance phase — most open work is human/ops-gated (marketing, email/DNS, fiscalization #297). For current open work, run `gh issue list --state open`.

### Organisation registry details

Use these when filling out third-party platform business/verification forms (Meta Business Manager, Stripe, Google Ads, etc.). **Authoritative source is the RNO register** (Registar neprofitnih organizacija, RNO broj `0163001`): https://banovac.mfin.hr/rnoprt/ — search by OIB. `fininfo.hr` mirrors it but lags, so cite banovac when a reviewer needs proof.

| Field | Value |
|---|---|
| Legal name | `HRVATSKO GLAZBENO DRUŠTVO SV.CECILIJA - KORČULA` |
| OIB (tax ID) | `52537805408` |
| MB (registry no.) | `03688194` |
| Registered address | `Knežev prolaz 1, 20260 Korčula, Croatia` |
| Phone | `+385 92 1532305` (the president's **private mobile** — fine on a registry form, never on a buyer-facing surface; see the hard rule below) |
| Legal form | Membership organisation (`Djelatnosti ostalih članskih organizacija`) |
| Date of establishment | 1991 (current Croatian legal entity; the society itself dates to 1883 — use 1991 for any "company founded" registry field) |
| Authorised representative | Velebit Veršić (President) |
| Website (for new forms) | `https://moreska.eu` |

RNO registry updated 2026-08-17 (#369): website `https://moreska.eu/`, e-mail `info@moreska.eu`. The `fininfo.hr` mirror still shows the old `www.korcula-moreska.com` and will catch up on its own — don't chase it.

### Stack

- **Framework:** Next.js 16 (App Router, TypeScript).
- **Styling:** Tailwind CSS + custom CSS (`src/app/globals.css`). Scope map + breakpoints under [CSS architecture](#css-architecture); gotchas in `docs/agents/frontend-css.md`.
- **i18n:** Locale is **not** in the URL. `src/proxy.ts` reads the `moreska_locale` cookie (or `Accept-Language` on first visit) and forwards it via an `x-locale` header; pages call `getLocale()` (`src/lib/locale.ts`). Translations in `src/messages/{en,hr}.json` (must stay structurally identical — dict type is inferred from `en.json`, so a key added in English and forgotten in Croatian type-checks and then renders `undefined`). Since #542 that is **enforced**, not just asked for: `src/lib/message-parity.test.ts` compares every key path and its shape across the two files, and it is where every future locale-pair assertion goes rather than beside the feature that added the key. Helper: `src/lib/i18n.ts`.
- **Fonts:** Brand identity Option 1 (picked 2026-06), loaded via `next/font/local` from `assets/fonts/brand/` in `src/app/(frontend)/fonts.ts`: Labrada (`--font-bodoni`, headlines/titles), Labrada (`--font-inter`, body), Neue Haas Grotesk Display (`--font-ibm-plex-mono`, codes/tags). Variable names kept for backwards compat with `globals.css`. The Payload admin (`(payload)/custom.css`) still pulls Bodoni Moda SC via the Google Fonts CDN for its own accents.
- **CMS:** Payload CMS v3 (`@payloadcms/next`, integrated into the Next.js app). Admin at `/admin`. Customization gotchas: `docs/agents/payload-admin.md`.
- **Database:** PostgreSQL via `@payloadcms/db-postgres` (`DATABASE_URL`, auth gated by `PAYLOAD_SECRET`). Dev and prod use **distinct databases** so a misconfigured `DATABASE_URL` can't cross over — current names + container topology in `docs/agents/deployment.md`. Throwaway `admin/admin` lives only on the dev DB; never copy to prod.
- **Payments:** Stripe (EUR). Payment Element (cards + Google/Apple Pay). Webhook `POST /api/stripe/webhook` (signature-verified). Keys: `STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, `STRIPE_WEBHOOK_SECRET`. The `korcula-moreska.com` → `moreska.eu` account migration is **finished**: there is exactly **one enabled** live endpoint (`we_1TZquZ…` → `https://moreska.eu/api/stripe/webhook`, subscribed to `payment_intent.succeeded`, `payment_intent.payment_failed`, `charge.dispute.created`, `charge.dispute.closed`); the legacy WooCommerce endpoint is **disabled** and must stay that way (verified via the API 2026-08-21). Adding events: runbook in `docs/agents/deployment.md`.
- **Email sending:** Brevo (free tier, 300/day). Sends from `info@moreska.eu` (review/newsletter mail from `newsletter@bilten.moreska.eu`). Key: `BREVO_API_KEY`.
- **Email receiving:** Google Workspace (ADR-0010, cutover #223 done 2026-08-13). MX is the single record `1 smtp.google.com`; `info@moreska.eu` is a real mailbox at `mail.google.com`. `tickets@` is a free alias on that user, and every unrecognized address catch-alls to it via Gmail → Default routing. **ImprovMX is retired** — no forwarding path survives, so any new `@moreska.eu` address must be an alias or it only works via the catch-all.
- **Analytics & ad tracking:** GA4 (`NEXT_PUBLIC_GA_ID`) loaded via `next/script` in `src/components/CookieConsent.tsx`, gated on consent. A single umbrella Google tag (`GT-*`) covers both GA4 and Google Ads — fire **one** `gtag('event', 'purchase', …)` on the confirmation page (don't load a second `gtag/js?id=AW-…` script). The `gtag()` helper must push the `arguments` object, not an Array. **Meta tracking:** browser Pixel (loaded in `CookieConsent.tsx`, gated on consent) + **server-side Conversions API Purchase** fired from the Stripe webhook (`src/lib/meta/capi.ts`), deduped by a shared `event_id = order_<orderId>` on both legs (`MetaPixelPurchase.tsx` sets the browser `eventID`). Verified live end-to-end 2026-07. **Paid campaign management (#45, #46) and marketing listings (#35, #36, #39) are owned by a separate HGD member, not the developer** — dev scope stops at tag firing + measurement correctness.
- **Infrastructure:** Hetzner Cloud + Coolify (Nuremberg). SSL automatic via Traefik (set the domain to `https://` in Coolify). See `docs/agents/deployment.md`.
- **DNS:** Hetzner DNS, managed through the Hetzner Cloud DNS API (the zone lives in the Cloud project, so a Cloud token with write scope edits it). Domain `moreska.eu` registered at Totohost; nameservers delegated to Hetzner. `app.moreska.eu` is live since 2026-09-13 (#479) and only ever answers a 301 to `https://moreska.eu/app` with the path and query preserved: `moreska.eu/app` stays the address, the short host exists for posters and spoken instructions. The redirect lives in code (`src/lib/app-subdomain.ts`, wired into `next.config.ts` `redirects()`), never in a hand-written Traefik label, because Coolify regenerates its labels whenever the domain list changes. Host inventory and how it was turned on: `docs/agents/domains.md`.

### Pages

| Route | File | Purpose |
|---|---|---|
| `/` | `src/app/(frontend)/page.tsx` | Homepage (10 sections) |
| `/about` | `src/app/(frontend)/about/page.tsx` | About HGD |
| `/sections/[slug]` | `src/app/(frontend)/sections/[slug]/page.tsx` | Section pages (see slug map below) |
| `/tickets` | `src/app/(frontend)/tickets/page.tsx` | Public show schedule (reads Shows; force-dynamic) |
| `/services/[slug]` | `src/app/(frontend)/services/[slug]/page.tsx` | Service enquiry pages, no pricing |
| `/checkout/[showId]` | `src/app/(frontend)/checkout/[showId]/page.tsx` | Stripe checkout |
| `/checkout/[showId]/confirmation` | `…/confirmation/page.tsx` | Post-payment landing; looks up Order by `pi` (5×400ms retry to bridge the webhook race) |
| `/privacy-policy`, `/cookie-policy` | `src/app/(frontend)/…/page.tsx` | Legal pages (EN+HR), via `LegalPage.tsx` |
| `/scan/[token]` | `src/app/scan/[token]/page.tsx` (+ `scan/layout.tsx`) | Auth-aware door scan — buyer view if unauth, staff atomic mark-and-read if internal. Outside `(frontend)` (own minimal layout). Logic in `src/lib/scan-token.ts`; CSRF caveats in `payload-admin.md` |
| `/app` (Početna), `/app/orders`, `/app/orders/[id]`, `/app/inquiries`, `/app/inquiries/[id]`, `/app/moreska`, `/app/moreska/[id]`, `/app/performances`, `/app/performances/[id]`, `/app/members`, `/app/members/[id]`, `/app/leaderboard`, `/app/leaderboard/full`, `/app/scan`, `/app/sell`, `/app/statement`, `/app/comp`, `/app/users`, `/app/users/[id]`, `/app/stats`, `/app/finance`, `/app/more`, `/app/account`, `/app/notifications`, `/app/welcome`, `/app/install`, `/app/session`, `/app/login`, `/app/forgot`, `/app/authorize`, `/app/join/[code]` | `src/app/app/…` (own route group + layout) | **Cecilija** (the app's name since #489; "moreškant" now names only the dancer and the roster section), Croatian only, noindex, outside `(frontend)`/`(payload)`. **The shell is #495:** the permission → screen table is `src/lib/app/screens.ts` and NOTHING re-types it — the phone bar, the laptop sidebar, the active-tab highlighter and every page gate (`openScreen()` in `src/app/app/gate.tsx`) read it, and a screen ticket's only change there is moving its permission from `unlockedBy` into `servesToday` — or, where the redesign splits one screen in two (#565's Moreška took `moreskant` out of Izvedbe's `servesToday` and opened a screen of its own), one new row and one permission moved back the other way, never a re-ranking of the rest. `/app` itself is **Početna** (#564), the landing screen every account that is in opens on: the logo and the wordmark (the one screen that names the app), a greeting with the reader's first name and no vocative, one sentence about the next nastup or izvedba in their own register, and one card per tab in the account's own order plus Obavijesti, four at most — a card whose screen the account does not unlock is never built, because the cards come off `nav.tabs` rather than off a second list, and no money appears on it except the one figure Financije already computes. Početna is the FIRST tab and Više the last, neither counted against the three and neither storable on `Users.tabs`; access is "the set unlocks at least one screen" (`src/lib/app/access.ts`); attendance answers write through `POST /api/app/attendance`; `/app/moreska` is **Moreška** (#565), the dancer's register screen and a `moreskant`'s first tab after Početna: their name, primary role and 44px mark, the hero of the next **nastup** with Dolazim / Ne dolazim in it, the ArmyBar that taps into Stanje, and the season by month. A non-regular evening reads *Vanredna* there and never names its client, no seat count or euro appears on it, and a dancer's answer never carries an army — one arriving from a dancer is ignored and the answer counts in their primary role's army. It is also where the **two registers** land (CONTEXT.md): a moreškant reads "nastup" on Moreška, in the attendance and lineup copy, in every push and on Ljestvica, while the blagajna reads "izvedba" for the same evening on Izvedbe. `/app/moreska/[id]` is **Stanje** (#566), one nastup on one screen: the ArmyBar, crni and bili as two columns with their empty places and a Bule card, **Odustali** above Bez odgovora (#612, CONTEXT.md *Odustajanje*: whoever said dolazim, let it stand ten minutes and took it back, with the hour and whether a voditelj wrote it down, disjoint from Ne dolaze and never reached by a push, because a dancer changing their mind is written under the evening rather than rung), and for a voditelj **Pozovi (with a bell) and Potvrdi postavu, in the flow under the Bule** (#620, moved out of a sticky bar that sat under the tab bar's blur zone and was read through it), plus a person sheet that answers on somebody's behalf, moves them between armies and hands out the evening's **titles** — a title lives in that evening's lineup and never on the profile (CONTEXT.md *Title*), each has exactly one holder. Potvrdi is pressed **after** the evening, not before it, because a postava records who danced. **What a confirmed postava must carry depends on the KIND** (#620, `lineupRequirements` in `src/lib/lineup/titles.ts`, applied under the shows row lock in `decideConfirmation`): every kind but the Experience needs all four titles, once each, and cannot hold a `voditelj` line at all; a **Moreška Experience** needs exactly one `voditelj` and no titles, gets a Voditelj card of its own on Stanje and is the only kind whose `LineupEditor` offers the role — both lineup writers, the MCP `set_lineup` included, refuse it anywhere else, and an unconfirmed write still passes every other check. **A dancer answers from the LIST as well as from the hero** (#624): every future row on Moreška carries two circles where the "bez odgovora" chip stood — green with the crossed swords, red with an X, both always drawn, the fill carrying the answer — and Stanje carries the hero's own pair under the date for a reader who has a linked Member and an evening that has not started. Pressing the filled circle again takes the answer back, and what that MEANS is the server's (`resolveUndo`): inside the ten-minute grace the row is deleted, past it the row becomes `ne dolazim` with the odustajanje stamps, so the new control cannot become a hole through which a dancer leaves a postava unseen. `undo` is therefore a third request beside `clear`, which stays the voditelj's unconditional erase. **A voditelj ADDS people from the screen** (#620): an empty "mjesto n" before the nastup, one "+ Dodaj u crne / bile / bulu" row after it, neither on a confirmed postava; three of the four pickers write an ANSWER (`POST /api/app/attendance`), because until it is confirmed the postava IS the answers with the titles laid over them, and only the voditelj's own line goes straight to the lineup. Every picker and the Bez odgovora sheet carry a diacritic-insensitive search and role filters drawn as **discs, not words**. **What the counts COUNT changes twice** (CONTEXT.md *Army count*): a confirmed postava is what the bar, the two heads and the Bule card count, which is why the *bez odgovora* chip disappears with it, and a nastup that has STARTED drops the shortfall line and the "od 8" entirely. **A new Experience starts at 3/3 rather than 8/8** (`DEFAULT_THRESHOLD`, applied by `newPerformanceRow` for both the app route and the MCP tool). With Stanje live the Izvedbe detail is back to `openScreen('performances')`, every roster push deep link points at Stanje, and Početna's hero card follows the same `heroView().href`, because a `moreskant` does not unlock Izvedbe, and since #620 **the hero card's body is a way into the Moreška tab** — a cover link under the answer pair, the ArmyBar and each half's head, never an anchor wrapped around them; `/app/leaderboard` is **Ljestvica** (#568), the season ranked and the reader's own year behind one segmented control, *Ljestvica* first: **one season is two lists**, Moreška (every kind Cecilija shows except the Experience) and Experience, each a podium of its top three, the rows to twenty and the reader's own row highlighted or, below the cut, pinned as a last row of its own, with `/app/leaderboard/full?season=&kind=` carrying the whole ranking on the server so nothing reflows under a thumb. Equal counts share a rank and the next skips, a cancelled evening counts for nobody, the split by kind lives on *Moja sezona* while the prekretnice (5, 10, 15, 20) were **deleted rather than moved** (#607, CONTEXT.md), **Experience is counted and not placed until its leader reaches five** (`RANKED_MIN_TOP`, #614: one morning was buying "3. mjesto od 70" and the same gold mark twenty-one moreške buys) and a count of zero holds no place at all (`rank: null`, so `tsc` makes every screen say what it draws for a dancer who has not danced), a row may carry the **niz** as an ember flame beside the nickname (#628, CONTEXT.md *Niz*: confirmed moreške in a row, crossing seasons, drawn only at 3 or more AND only while the run is still going, computed by `src/lib/app/niz.ts` off its own cross-season read and never ranked by), the profile's record is **Najduži niz** where "Najbolja sezona" was, and **no mark on it wears a crown**: a titula belongs to one evening's lineup and never to a person, so the disc is the profile's army and the title is null. The rules are `src/lib/app/leaderboard-rank.ts`, pure and tested without a database; `/app/performances` is **Izvedbe**, the box office's register of the season and the voditelj's too, one screen whose content is decided by `can()` (#502, #503, redesigned by #567): every izvedba of the season, public and non-public, for both halves, with sold of capacity and the channel split under every public row and the per-show numbers the old `/admin/stats/[id]` drill-down had (revenue only for `finance`, and since #538 that *Prihod* is online money net of refunds plus the evening's ledger, with the partner seats named under it at face value before commission, never summed into it and never called a potraživanje, because that word is Financije's net-of-commission `netCents`). **The detail's title is the DATE** (#567, Q32) with the kind and the house under it, and nothing dancer-facing is left on the screen — a dancer answers on Moreška and reads the armies on Stanje — except the `LineupEditor`, which stays until Stanje can record an unusual role (#566). **Dodaj and Uredi are either half's since #567** (Q53): `tickets` and `moreska` both create and edit both kinds of row through the two shared routes `/api/app/performances` and `…/[id]`, which decide what may change from the ROW rather than from the caller and share their validator and their writer with the MCP `create_performances` tool; `POST …/[id]/cancel`, the booking's own, stays `moreska`. **The six named actions are gated one at a time, in the handler and never per row** (#567): Pauziraj, Pomakni datum, Preseli u zimsko, Prodaja na vratima and the Narudžbe link need `tickets`, **Otkaži needs `tickets` AND `refunds`**, and a reader without the word sees the button **greyed with "traži Blagajnu" rather than hidden** while the route refuses the same request — the map from a permission set to the six answers is `src/lib/app/performance-actions.ts` and nothing re-types it. Five actions reuse their existing route unchanged (`/api/shows/[id]/{cancel,reschedule,move-to-indoor,offline-sales}` and the Narudžbe link) while `POST /api/app/performances/[id]/pause` is the one that had none; a dancer's login is issued by the invitation (`POST /api/app/invite`), never self-registered; `/app/authorize` is the MCP connector's consent screen (#438); `/app/welcome` is the once-per-device walkthrough (#457, glossary *Dobrodošlica*, *Ljestvica*); `/app/install` is the install guide and one of the two `/app` pages outside the access decision, because it is the QR target for a rehearsal (#455); **the install is OFFERED on Početna and asked of the browser, never of a flag** (#616): `src/lib/app/install-nudge.ts` reads `display-mode: standalone`, so the card is gone the moment the app is on the home screen and needs nothing written to disappear, and it is shown to every account that is in rather than only to a dancer — the Dobrodošlica keeps its `hasMember` gate because its push and calendar steps are written in the dancer's register, and finishing it snoozes the card for a day. A desktop browser is offered it only when it fired `beforeinstallprompt`, because Safari and Firefox have no install for the guide to describe; `/app/scan` is **Skener**, the door's screen (#504): the camera, the four result states, the manual-admit search and the "ušlo X od Y" ring, with `/admin/scan` 308ing to it; `/app/sell` and `/app/statement` are **Prodaja** and **Obračun** (#505), a port of the Backoffice partner dashboard onto the five existing `/api/partner/*` routes (sell, cancel, cancel/undo, sales, reconciliation) — a partner login with no Partner link or a deactivated one gets a sentence and no figure, and Partners CRUD plus partner logins stay in the Backoffice. **Obračun is one document with two readers since #599**: the reseller reads what they owe, and `finance` picks a partner and a month in the query string (`?partnerId=&year=&month=`, server-rendered) and reads the same document, because "usporediti je li sve okej" is impossible while the secretary works from a different artifact. It is **Croatian throughout** — a PDF (`renderStatementPdf`, a sibling of the ticket renderer) as the thing that gets attached, and an Excel-safe CSV (BOM + `;` + decimal comma) beside it; one row per izvedba, the period spelled as the SALE days it covers, **Za uplatu HGD-u** as the only figure a račun is raised from, storno on a line of its own that is in no sum, and **no IBAN or payment instruction**, because the partner pays against the accountant's račun. **Označi poslanim freezes the month** (`partner_statements`, `POST /api/app/statement/sent`, a switch `{ sent: true|false }` gated on `finance` and refusing a month that has not ended); Cecilija never sends the mail, it hands over a `mailto:` and the PDF, the way Upiti does. `Vrijednost prodanog` is the one name for the gross, here and on Financije (CONTEXT.md); `/app/stats` is **Statistika** (#508), the season in counts for `tickets`, `season_stats` and `finance`: the season band, one row per public izvedba with its channel split, the trajectory and channel-mix charts moved as-is from the Backoffice dashboard, and "gratis po članu" (#506's own tally, read through `repo.comp`) for `tickets` only — **never money on this screen for anybody**, because euros are Financije (#509, decided in #500), and a row links into `/app/performances/[id]` only for a reader who unlocks Izvedbe, so the shared `member` login (`season_stats`, its only screen) reads the numbers with no link that would refuse itself; `/admin/stats` and `/admin/stats/[showId]` 308 to `/app/stats` and `/app/performances/[showId]` through `src/lib/backoffice-ports.ts`, which is also where #504's `/admin/scan` entry now lives; `/app/finance` is **Financije** (#509), the other half of the season and the only Cecilija screen that prints euros: prikupljeni prihod (**`channel='online'` only**, net of refunds, plus the offline ledger) in one card and potraživanje od partnera in another, **never summed and never called dobit** (ADR-0015, glossary *Dashboard*) — a partner order stores `total` at face value the society has not collected and a storno leaves both `total` and `refund_status` untouched, so the channel clause is the correctness of both cards and not an optimisation (it lives in `revenueCollectedCents` since #538, so the Backoffice season band and the per-show *Prihod* on Izvedbe apply the same rule, though not over the same rows: the band is all-time and unscoped to public performances, this card is one season; a `CollectedOrderRow` carries the channel as a REQUIRED field, so a caller that forgets to read it fails `tsc`), povrati as a count and an amount (a lost chargeback included, because #380 writes the same `refund_status`), promo revenue as an "of which" line, the per-partner receivable for a month the picker caps at the current Zagreb month with the statement CSV behind `/api/partner/reconciliation?partnerId=&year=&month=&format=csv`, and the season's offline ledger by evening with every line's price and discount label — **and no buyer name or e-mail anywhere on it**, asserted by a source scan (`src/lib/app/finance-no-pii.test.ts`); `/app/orders` is **Narudžbe**, the blagajna's order list and one order (#501): search, a performance and a state filter and a pager, all in the query string, and four named actions on the detail — Povrat (`refunds` only), Pošalji ulaznice ponovno, Otvori PDF and Uredi kupca, whose `PATCH /api/app/orders/[id]/buyer` is the screen's one write; `/app/inquiries` is **Upiti**, the enquiry inbox (#507): unanswered first with booking enquiries lifted above general ones, a `state` filter and a pager in the query string, the whole message on the detail, **Odgovori** as a server-built `mailto:` (Cecilija never sends the mail; Tatjana answers from `info@` in Gmail) and **Označi riješenim** through `POST /api/app/inquiries/[id]/handled`, a switch rather than a toggle so the undo is the same button and a retry is harmless; `/app/comp` is **Gratis** (#506), where a `tickets` holder hands a society member free seats: Podijeli gratis (a required member picker with an inline Dodaj člana, the two steppers, a prefilled holder name) on the existing `POST /api/comp/issue`, Poništi gratis on a whole order or one ticket on the existing `POST /api/comp/cancel` behind a confirmation (that route has no undo, so none is offered), and the Gratis po članu season table — promo codes stay in the Backoffice; `/app/members` is **Članovi** (#511), the voditelj's roster screen, which absorbed Pozivnice: the rehearsal join code and its pending claims, Dodaj plesača, and a searchable list (diacritic-insensitive, active moreškanti first) whose every row carries the two invitation channels, the **three marks** of #653 (📲 installed, 🔔 notifications, 🔑 *Pristup*, one glyph per column with the state in its weight and a rule for *ne znam*, or the words *nije ušao* for a dancer who has never signed in) under the four counting chips `svi · bez appa · bez obavijesti · bez pristupa`, and links to the profile at `/app/members/[id]`, where nadimak, mobitel, plesne uloge, glavna uloga and aktivan are written through `PATCH /api/app/members/[id]` and a new dancer through `POST /api/app/members` — both guarded by `requirePermission(req,'moreska')`, both running the Members `beforeValidate` hook through the seam so nickname uniqueness stays the collection's, and `name`/`note` refused in the route because they are the ADR-0019 attribution half; `/app/invitations` and `/app/pozivnice` both 308 there, and deleting a member stays in the Backoffice; `/app/users` is **Korisnici** (#510, #563), where a `users` holder reads every account as Croatian permission chips and changes it through eight named actions — Dozvole, Resetiraj lozinku, Ime, E-mail, Poveži partnera, Poveži člana, Tabovi, Dijeljeni račun — plus Novi korisnik on the list. **Tabovi is #563's**: the three screens this account's bar carries, stored on `Users.tabs`, refused on a screen the account does not unlock, because a tab is an order and never a permission, and since #591 allowed on the caller's OWN row (a `users` holder arranges any bar, their own included); the generic order for everybody nobody has chosen for lives beside the table in `src/lib/app/screens.ts`. Six of its eight routes ARE the lock, because `permissions`, `shared`, `tabs` and `member` are field-locked and the seam writes with `overrideAccess`: each re-checks `users` in the handler, refuses the caller taking `users` off their own row (409), refuses a `shared` login as the CALLER on all eight (403, wider than the collection's self-edit rule, because the `tehnika` password is on a wall) and refuses both a named-person set on an address-less account (400) and `users` on a shared one (409). **The other two write fields nobody locked, and only one of them is cosmetic.** Ime (#617) is the account's own `name`, empty meaning `null` because `tehnika` is a room and not a person, and it is the one action with no self refusal — correcting the spelling of your own name grants you nothing, which is why the field is unlocked in the collection at all. **E-mail (#621) is an auth field**: it is what a sign-in link and a forgotten password are addressed to, so clearing it is refused for a set `emailRequiredFor` covers (400, naming Dozvole rather than the Backoffice, which is what that refusal used to say), an address that already belongs to somebody is a 409, the row's own address is a no-op rather than a self-collision, and **no mail is sent** — Cecilija never tells a person their address changed, the same way it never sends the invitation. `normaliseEmail` and `normaliseName` live in `users-admin.ts` so the create and the two PATCHes keep one definition of each. A new or reset login is handed over one of two ways and never a third: a one-hour sign-in link when the account has an address AND a set that unlocks a Cecilija screen (`mayOpenAppSession`, the same rule `POST /api/app/session` applies — the two have to agree or the link is refused by the handler that minted it), a temporary password shown once otherwise. Linking a member carries #487's `taken` / `not-active` refusals and writes the link only; deleting an account stays Backoffice work; `/app/more` is **Više** (#495, redesigned by #569), which opens with the PERSON rather than with a list: a moreškant's RoleMark (the army of their primary role, and never a title, because a title belongs to an evening) or, for everybody else, their permission set as the Croatian chips Korisnici renders through `permissionChips` — a shared login is named by its username with a *Dijeljeni račun* chip, because `tehnika` is a room. Then four rows of their own (Profil · Obavijesti with the unread count · Sigurnost, which is `/app/account#security` and not a route of its own · Podrška, a sheet carrying `info@moreska.eu` and never a telephone number), then the overflow screens with the same icons their tabs carry, then the app's own rows, Odjava, and the build line **whose commit is the one `/api/health` reports** (`deployedCommit()` is the single env read, asserted in `src/lib/app/build.test.ts`). **That line carries NO version and #637 decided it never will**: `package.json`'s `0.1.0` had not moved since the repo's first commit, and nothing replaced it, because `main` auto-deploys at roughly twenty merges a day, so any scheme writing a version into `package.json` costs a bot commit and a second deploy per merge while the number itself says nothing at that tempo. The sha is not a thing to READ ALOUD (`0`/`O`, `b`/`6`, the hazard CONTEXT.md's temporary-password alphabet exists for), so it leaves the phone two other ways off ONE `deployedCommit()` read: a tap copies the whole line, and Podrška's `mailto:` carries the build AND the signed-in username in its body (`src/lib/app/support-mail.ts`), because `tehnika` has no address of its own and a mail about the door tablet otherwise never says which account was in. A window left open ACROSS a deploy is #638, not this; `/app/account` is **Profil** (#569, renamed from "Moj račun" so the row and the screen agree), where the two switches live beside the self-link screen and **E-mail i lozinka** (#651, [ADR-0028](docs/adr/0028-moreskant-owns-their-access.md)): one form under `#security` writing the reader's OWN `Users.email` and password through `PATCH /api/app/account`, whose HANDLER is the lock (the row is the session's own id, and `name`, `note`, `permissions`, `member`, `shared`, `tabs` and `username` are refused outright), plus a card on Početna that names both halves and disappears the moment the account carries an address — `Members.email` and "the Member's e-mail wins" are retired with it, so nothing overwrites what a dancer typed, and `/app/welcome` gains no fourth step because the Dobrodošlica is per DEVICE and this is per account. The two switches themselves: the **theme switch** (Svijetla / Tamna / Kao sustav, Svijetla being the default since #633 and a stored *Kao sustav* still following the phone, kept per DEVICE in `localStorage` under `cecilija.theme` and applied before the first paint by the inline script in the route group's layout — `src/lib/app/theme.ts` owns the rules and the boot script both, so nothing re-types the key) and **push as a switch** rather than a banner with a flipping label, whose three refusals (an iPhone that is still a Safari tab, a webview, a browser with no push) are a Note that says what to do and never a dead control. It is also where a voditelj who dances links their own Member, the one sanctioned way past the `Users.member` field lock (#462); `/app/notifications` is the *Sandučić obavijesti* behind the header bell (#496), a Više row rather than a tab, open to every account the access decision admits, with *Označi sve pročitanim* as quiet text on its section heading and the calendar feed as a panel UNDER the list (#569). **It is also where a voditelj WRITES to the roster** (#654, ADR-0028): a seventh `AppNotificationKind`, `message`, and the first that is not event-driven, free Croatian text to every active moreškant with no recipient picker. The action is gated per ACTION on `moreska` and **hidden**, not greyed with "traži Moreška" (that rule is Izvedbe's and exists so a blagajna knows who to ask), while `POST /api/app/notifications/message` re-checks the permission with `requirePermission` regardless. It goes through `createSender` like every other roster sender, so the inbox rows are filed for the whole audience before a single endpoint is posted to and a push that fails in part costs nobody their row; the send is refused without `confirmed: true`, because the confirmation sheet is where **both numbers are said out loud** ("Zvonit će na 43 mobitela, a svih 76 će je naći u Sandučiću", computed on the server by `getRosterMessageCounts`) and those counts are the receipt that stops the same sentence being sent twice. The rules are `src/lib/push/roster-message.ts`; **since #463 an invitation LINK is what signs a dancer in** (`/app/session?token=…` → `POST /api/app/session`), a voditelj hands invitations over by SMS from `/app/members`, and `/app/join/[code]` is the rehearsal QR — the other page outside the access decision, public because the person scanning has no login yet, and the only public page that WRITES (a claim a voditelj approves); see `docs/agents/moreskant-app.md`. **The Croatian segments were renamed by #495**, each with a 308 in `next.config.ts`; two of them (`instalacija`, `prijava`) are permanent because a QR on a wall and an SMS link cannot be edited. Shape and scope of the app: [ADR-0027](docs/adr/0027-cecilija-one-staff-app-payload-backoffice.md) |
| `/api/mcp/[transport]` | `src/app/api/mcp/[transport]/route.ts` | MCP server for the Claude app (ADR-0024, #438): five roster tools behind hand-written OAuth 2.1 (`src/lib/mcp/`, discovery under `src/app/.well-known/`). See `docs/agents/moreskant-app.md` |
| `/admin` | Payload + `src/components/payload/AdminDashboardView.tsx` (and `AdminStatsView.tsx` / `AdminShowStatsView.tsx`, still mounted but unreachable) | Backoffice dashboard. **`/admin/stats` and `/admin/stats/[showId]` 308 to `/app/stats` and `/app/performances/[showId]` since #508** (`src/lib/backoffice-ports.ts`); the two stats views stay in the tree until #512 retires the Backoffice screens |
| `/api/stripe/webhook` | `src/app/api/stripe/webhook/route.ts` | Creates Order + Tickets on payment success |

### Key files

| Path | Purpose |
|---|---|
| `src/proxy.ts` | Locale detection (Next.js 16 "proxy" convention) |
| `src/lib/shows.ts` | **Only** server-side entry point for frontend show data: `getUpcomingShows()`, `getNextShow()` (canonical "active door show"); derives `remaining` from `VENUE_CAPACITY` |
| `src/lib/scan-token.ts` | Pure DI logic for `/scan/[token]` → `VALID \| ALREADY_SCANNED \| INVALID`; race-safety delegated to `deps.atomicMarkScanned` (raw `UPDATE tickets … WHERE scanned=false RETURNING …`) |
| `src/lib/access/permissions.ts` | The permission vocabulary + `can()` / `hasAny()`: source of truth for access (ADR-0023) |
| `src/lib/repo/` | The repository seam between Cecilija and Payload (phase A, ADR-0027 decision 5): `getRepo()` above, `repo/payload/` below. Phase B reimplements `repo/payload/` and nothing above it; `repo-guard.test.ts` is what keeps new `/app` code out of Payload |
| `src/lib/access/partner.ts` | Partner ownership `Where` scoping, shared by collection access + partner routes |
| `src/lib/data.ts` | Locale-agnostic data: performances, history vignettes, section/service card + page meta |
| `src/messages/{en,hr}.json` | All UI strings (identical structure) |
| `src/app/globals.css` | Full design CSS + Tailwind + breakpoints |
| `src/app/(frontend)/layout.tsx` | Root layout for the public site — fonts, `lang`, `CookieConsent` |
| `src/components/*` | `Nav`, `Hero`, `PageHero`, `LegalPage`, `CookieConsent`, `ServiceEnquiryForm`, `Contact` (most are `'use client'`) |
| `docs/` | `copywriting{,-hr}.md`, `design-brief.md`, `performances.md`, `sveta-cecilija.md`, `todo.md` |

### Assets

Production assets live in `public/` (the served set). Key files: `hero-horizontal.webm` / `hero-vertical.webm` (+ matching `*-poster.webp`), `cecilija-logo.webp`. Originals stay in `assets/images/`; the PDF renderer needs the PNG logo at `assets/images/cecilija-logo.png`. **Any photo in `public/` must be optimized webp.** Full conventions + `cwebp` recipe: `docs/agents/assets.md`.

### Payload CMS collections

Field-level detail lives in `src/collections/*.ts` — this table is purpose + key relationships only.

| Collection (slug) | Purpose |
|---|---|
| `Shows` (`shows`) | **Every** performance, public or not (ADR-0024): `kind` + `isPublic` decide whether a row sells tickets. Public rows keep the old shape (date/time/venue, sold counters, `status`, #94 venue-move audit); capacity derived per venue, never stored. Rules: `docs/agents/features.md`. |
| `Orders` (`orders`) | One purchase: buyer + counts + `total` (EUR cents) + `stripePaymentIntentId` + `refundStatus` → Shows. `channel` (`online \| partner \| comp`); `partner` link for reseller scoping; `member` link for comp attribution (ADR-0019); `promoCode` link for online promo orders (ADR-0018, still `channel=online`); `compIssuedBy` (`admin \| self`, NULL reads as `admin`) marks a comp a moreškant issued for themselves from `/app` (#434, capped at 4 per performance) |
| `OrderLookups` (`order-lookups`) | Buyer-facing order lookup support |
| `Tickets` (`tickets`) | **Per-person** ticket + QR token → Orders; `scanned`/`scannedAt`. Seats = COUNT of active tickets (`online_sold` retired). Renamed from `qr_tokens`. |
| `ContactSubmissions` (`contact-submissions`) | Enquiry-form submissions |
| `Users` (`users`) | Payload auth + `permissions` (see `permissions.ts`) + `shared` + an optional `name` (#510: whom the login belongs to, empty on a shared one). Hybrid username login (ADR-0011): unique `username` (field-locked to `users` since #424), email optional but required for `users` / `tickets` / `moreska` / `finance` / `editor` holders (a `moreskant` needed one until #463, when the invitation became a link a voditelj can send by SMS). Shared door account is username `tehnika` (no email), shared society login is `member`. A `partner` holder carries a `partner` → Partners relationship; a dancer's login carries a `member` → Members one (`docs/agents/moreskant-app.md`). |
| `Partners` (`partners`) | Reseller channel (ADR-0008): `name`, `oib`, `billingAddress`, `email`, `commissionPercent`, `active`. `tickets` CRUD; a partner reads only its own record. `email` is the SETTLEMENT contact (#599), deliberately not the linked login's address: that account belongs to whoever stands at the counter, while the obračun goes to the agency's accounting mailbox. |
| `Members` (`members`) | Society members (ADR-0019): `name`, `active`, `note`. Shared attribution target for comp tickets (`orders.member`) and promo codes (`promoCodes.member`). Since #420 also the moreškant identity (ADR-0024): `isMoreskant` + nickname, mobile, dance roles, primary role, locked to `moreska`; rules in `docs/agents/moreskant-app.md`. **`email` was retired by #651** ([ADR-0028](docs/adr/0028-moreskant-owns-their-access.md)): its only job was "where the app invitation is sent", the address now lives on the dancer's own login, and the line is that **the mobile is the voditelj's channel and the e-mail is the dancer's access**. |
| `PromoCodes` (`promo-codes`) | Member promo codes (ADR-0018): `code` (unique), `member` (→ Members), `discountType` (`adult-price-override`), `adultPriceEur` (default 15), `active`. Applied at online checkout, best-of-two vs 5-for-4. `tickets` CRUD. |
| `Attendance` (`attendance`) | One moreškant's answer for one performance (ADR-0024, #422): `performance` → Shows, `member` → Members, `status` (`coming \| not_coming`), `army` (`crni \| bili \| null`), `answeredBy`, `answeredAt`. Unique on (performance, member); **"no answer" is the absence of a row**. Written only by `POST /api/app/attendance`; rules + army count in `docs/agents/moreskant-app.md`. |
| `Lineups` (`lineups`) | The postava of one performance (ADR-0024, #432): `performance` → Shows, `member` → Members, `role` from the dance-role enum. Unique on (performance, member); confirmation is one flag per evening (`shows.lineupConfirmed`), and only confirmed lineups reach a dancer or the season statistics. Written only by `POST /api/app/lineup`; rules in `docs/agents/moreskant-app.md`. |
| `Posts` (`posts`) | Blog posts (heroImage may be a remote URL). Gated by `editor` since #500, not `tickets`. |
| `offline_sales` | **Raw table, NOT a Payload collection** (ADR-0025): the append-only ledger of seats sold with no order and no ticket. One line = `source` (`door \| legacy`) + `ticket_type` (`adult \| child`) + `quantity` (may be negative, that is how a miscount is corrected) + `unit_price_cents` + optional `discount_label`. `shows.in_person_sold` / `shows.legacy_reserved` are its cached per-source totals. Money and the adult/child split read the ledger, never the counters. `db/schema/migrate-zz-da-offline-sales.sql`. |
| `marketing_optouts` | **Raw table, NOT a Payload collection** (#57): `email` PK, `source`, `optedOutAt`. Created in `db/schema/app.sql`; see `docs/agents/features.md`. |
| `push_subscriptions`, `performance_notifications` | **Raw tables, NOT Payload collections** (#431, #435): the roster's devices and its notification claims. `db/schema/migrate-push.sql`; see `docs/agents/moreskant-app.md`. |
| `oauth_codes`, `oauth_tokens` | **Raw tables, NOT Payload collections** (#438): the MCP connector's credentials. `db/schema/migrate-zz-d-oauth.sql`; see `docs/agents/moreskant-app.md`. |
| `app_join_codes`, `app_join_claims` | **Raw tables, NOT Payload collections** (#463): the rehearsal join code and the claims a voditelj approves. `db/schema/migrate-zz-db-join.sql`; see `docs/agents/moreskant-app.md`. |
| `app_devices` | **Raw table, NOT a Payload collection** (#652, ADR-0028): one row per BROWSER that has opened Cecilija, named by a random id in a server-written `HttpOnly` cookie (`cecilija_device`, `src/lib/app/device.ts` — script-written cookies die in seven days under Safari's ITP). Written only by the keeper POST, at most once per six hours per device; `standalone` means "this browser has reported itself installed" and is ORed rather than replaced. It answers *Instalirano* with `unknown` as a first-class value — the table starts empty and nothing backfills an install from a user agent — while *Obavijesti* is read from `push_subscriptions`, true from day one. `db/schema/migrate-zz-dl-app-devices.sql`; `src/lib/app/device-store.ts` is its only reader and writer. |
| `app_notifications` | **Raw table, NOT a Payload collection** (#496): the Sandučić obavijesti, one row per account per notification (never an order). `db/schema/migrate-zz-dd-app-notifications.sql`; see `docs/agents/moreskant-app.md`. |
| `partner_statements` | **Raw table, NOT a Payload collection** (#599): one row per (partner, month), written only when a month is marked sent, holding the whole statement as JSONB plus the commission percent it was billed at. Marking sent FREEZES the month — before it, commission was applied at read time, so a rate change rewrote months the accountant had already invoiced. Nobody edits one by hand, which is the point. `db/schema/migrate-zz-di-partner-statements.sql`; `src/lib/partner/statement-store.ts` is its only reader and writer. |

### Permission-based access controls

Every decision reads the permission set via `can()` / `hasAny()` from `src/lib/access/permissions.ts` (ADR-0023). Partner ownership scoping lives in `src/lib/access/partner.ts` and is re-derived by partner-facing routes, because the local API runs `overrideAccess: true`. Vocabulary, bundles and gotchas: `docs/agents/permissions.md`.

| Collection | read | create/update/delete |
|---|---|---|
| `Orders` | `tickets`; `partner` → only own (`partnerOwnOrdersWhere`) | `tickets` |
| `ContactSubmissions` | `tickets` | `tickets` |
| `Shows` | `tickets` or `moreska`; `door` → public performances only | `tickets`; `moreska` → non-public rows, plus the roster fields on any row (#408) |
| `Tickets` | `tickets` or `door`; `partner` → only own (`partnerOwnTicketsWhere`) | `tickets` |
| `Partners` | `tickets`; `partner` → only own (`partnerOwnRecordWhere`) | `tickets` |
| `Members` | `tickets` or `moreska` | `tickets` or `moreska`; delete `tickets` only. Moreškant fields read+write locked to `moreska`; `name`/`active`/`note` update locked to `tickets` — except that `active` on a moreškant row is also written by `PATCH /api/app/members/[id]` for `moreska` (#511), the one deliberate hole, narrowed to dancer rows and enforced in that route because the local API bypasses field access |
| `PromoCodes` | `tickets` | `tickets` |
| `Lineups` | `moreska`; `moreskant` → only rows of a confirmed performance (`lineupReadAccess`) | `moreska` only (`lineupWriteAccess`), a boolean for the same reason. Dancers never write a lineup |
| `Attendance` | `moreska`; `moreskant` → only own rows (`attendanceReadAccess`) | `moreska` only (`attendanceWriteAccess`) — never a `Where`, which Payload's create would read as plain "allowed". Dancers write through `POST /api/app/attendance`, where the rules live |
| `OrderLookups` | `tickets` | `tickets` |
| `Posts`, `Faqs` | public, published rows only; `editor` also sees drafts | `editor` (not `tickets`, since #500) |
| `Users` | `users`, else own row only | create/delete `users`; update `users` or self, except a `shared` account, which may never edit itself |

`Users.permissions` and `Users.shared` are additionally field-locked to `users` (read, update and create); the `Users.partner` link is write-locked to `users` with read left open, and the `Users.member` link is locked for read *and* write, so no one can grant themselves anything (or repoint themselves at another dancer) through the self-edit path. The legacy `role` column and its enum were dropped in #398.

`POST /api/orders/[id]/refund` and every other staff route re-check in-handler through `requirePermission` and 403 otherwise (the local API's `overrideAccess: true` means collection access alone doesn't gate them). The Stripe webhook and frontend show queries use the local API, so collection access doesn't affect them.

### Ticketing rules

- **Prices:** €20 adult, €10 child (fixed). **PDV-inclusive at 25%** (€20 = €16 base + €4 PDV), same rate both categories — confirmed by the accountant, may drop pending a ministry opinion, so treat the rate as configurable rather than a constant (ADR-0020).
- **Venue capacities:** `ljetno-kino` (Summer Cinema / Ljetno kino) = 350; `zimsko-kino` (Cultural Center Korčula / Centar za kulturu) = 250. (Ljetno kino was recorded as 320 until PR #448; the house holds 350 — [ADR-0025](docs/adr/0025-offline-sales-ledger.md).) Always derived from `VENUE_CAPACITY` in `src/lib/shows.ts`; remaining = capacity − sold tickets.
- **Public venue names differ from DB values** — EN "Summer Cinema" / "Cultural Center Korčula", HR "Ljetno kino" / "Centar za kulturu". Keys: `schedule.venue*`, `performancesPage.venue*`. Buyer-facing names come from `VENUE_LABEL` in `src/lib/venues.ts`. Venue shown on every show card; a bad-weather note tops the tickets page (zimsko is the fallback).
- **Every performance is a `shows` row; only `isPublic` ones sell tickets** (ADR-0024) — filter every buyer/partner/door/stats query through `src/lib/show-performance.ts`, never by spelling `isPublic` yourself. Rules: `docs/agents/features.md`. `docs/performances.md` is a frozen print, not the source of truth.
- **QR codes:** generated server-side at order creation, one per ticket, each encoding `https://moreska.eu/scan/[token]`. Door scanning is the browser-based `/scan/[token]` page only (Pretix dropped from MVP).
- **Comp & promo:** admin-issued free tickets ride `channel='comp'` (`total=0`, `orders.member` attribution, kept out of revenue, capacity-guarded like a partner sell; ADR-0019). Member promo codes apply at online checkout (`adultPriceEur` override, best-of-two vs the automatic 5-for-4, never stacking; server recomputes; ADR-0018) and stay `channel='online'`.
- **Refunds:** admin-initiated, plus buyer self-serve on a rescheduled show (ADR-0021: token-authed `/order/[token]/refund`, eligible while unscanned). Both *money-moving* paths share one idempotent, safely re-runnable engine — the route checks `refundStatus` before calling Stripe, the Stripe call carries a stable `refund:<paymentIntentId>` idempotency key (`src/lib/refund/create-stripe-refund.ts`), and a retry on an already-`refunded` order re-voids any still-active tickets (self-heal). Regression probe: `scripts/probe-refund-void.mjs`.
- **A lost chargeback is a third writer of `refundStatus='refunded'` and it deliberately bypasses that engine** (#380, `src/lib/dispute/handle-dispute.ts`): the funds already moved when Stripe decided the dispute, so calling the refund engine would double-refund or error. It mirrors only the bookkeeping half (mark refunded → cascade-void tickets) and sends no buyer email. Idempotency there is an **atomic claim** on `critical_events` (`INSERT … ON CONFLICT DO NOTHING`, partial unique index on `(kind, context->>'disputeId')`), *not* `recordCriticalEvent` — that writer swallows its own failures by contract, which is right for a reporting sink and fatal for a guard.

### CSS architecture

Gotchas (specificity, `backdrop-filter`, hero loading, `next/image`, route-group layouts) live in `docs/agents/frontend-css.md`. The scope map:

- **Theme tokens** in `.t-stone` — `--bg`, `--gold`, `--pad`, `--sectionPadX`, `--maxW` (1480px).
- **Homepage** (`.hp .t-stone`): `.about`, `.opera`, `.hist`, `.secs`, `.svcs`, `.contact--dark`, `.foot--atmos`.
- **Inner pages** (`.inner-page .t-stone`): `.page-hero`, `.ip-*`, `.sp-*`, `.svc-page__*`, `.legal-page__*`, `.vignette`, `.ip-cta`. Don't mix the `.hp` and `.inner-page` scopes.
- **Global:** `.cookie-banner*` (fixed bottom bar, hardcoded colours). **Nav:** `.nav` / `.nav--inner` / `.nav__hamburger` / `.nav__overlay`.
- **Breakpoints:** 1280 → 1024 → 768 → 480px. At 768px the hamburger shows, desktop nav hides, grids collapse. Hero uses `min(940px, 100vh)` — never hardcode a height taller than a laptop viewport.

### Slug mappings

`SECTION_PAGE_META` / `SERVICE_PAGE_META` in `data.ts`:

| Section slug | `sectionKey` | | Service slug | `cardIndex` |
|---|---|---|---|---|
| `moreska` | `moreska` | | `private-moreska` | 0 |
| `wind-orchestra` | `band` | | `moreska-experience` | 1 |
| `klapa` | `klapa` | | | |
| `choir` | `choir` | | | |

`Sections.tsx` has a local `KEY_TO_SLUG` map for building card hrefs.

### Deferred (post-season)

Payload CMS content management; bulk show-cancellation refunds; buyer email CSV export; bulk email to ticket holders per show; SEO `generateMetadata` on all pages; German language support.
